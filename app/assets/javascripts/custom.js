function updateUser(userId){
	debugger
  var $li = findItem(itemId);

  // var newLabelValue = $("input.edit", $li).val();

  // $("label", $li).html(newLabelValue)
  // hideEditInPlace($li);
}

// $(function(){
  // $("ul.list").on("dblclick", "li label", Item.edit)
  // $("ul.list").on("blur", "li input.edit", Item.stopEdit)
// });
