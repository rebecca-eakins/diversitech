<% unless current_user == @user  %>
  <div id="follow_form">
    <% if current_user.following?(@user) %>
      <%= render 'connections/unfollow' %>
    <% else %>
      <%= render 'connections/follow' %>
    <% end %>
  </div>
<% end %>